{
  "name": "hoco-sports-almanac",
  "version": "1.0.0",
  "private": true,
  "description": "HoCo Sports Almanac static frontend with Netlify function hooks for admin publishing and Stripe subscriptions.",
  "scripts": {
    "build": "node scripts/build.cjs",
    "start": "python3 -m http.server 4173",
    "serve": "netlify dev",
    "test": "node --test"
  },
  "dependencies": {
    "@netlify/blobs": "^8.2.0",
    "@netlify/identity": "^2.0.0",
    "sanitize-html": "^2.17.6",
    "stripe": "^17.5.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
